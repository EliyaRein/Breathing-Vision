# Breathing-Vision runtime dependencies (Python 3.10+ recommended)
# Install:  pip install -r requirements.txt

# --- core numerics / DL ---
torch>=2.0
numpy>=1.24
scipy>=1.10

# --- video, motion & ROI detection ---
opencv-python>=4.8
ultralytics>=8.0        # YOLOv8 person-box ROI detection

# --- dataset building & labels ---
h5py>=3.8               # AIR-400 respiration ground-truth (.hdf5)
pandas>=2.0             # QC tables
openpyxl>=3.1           # .xlsx export used by QC
gdown>=5.1              # fetch the AIR-400 dataset folder from Google Drive

# --- training / model selection ---
scikit-learn>=1.3       # GroupKFold subject-wise splits
optuna>=3.3             # inner hyper-parameter search

# --- deployment app & figures ---
PyQt5>=5.15             # live desktop application (deploy/app.py)
matplotlib>=3.7         # docs/make_diagrams.py schematics
